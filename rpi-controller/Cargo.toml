[package]
name = "rpi-controller"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
serialport = "4"
# rppal = "0.19"  # Descomentar cuando compilemos en la RPi
tokio = { version = "1", features = ["full"] }
