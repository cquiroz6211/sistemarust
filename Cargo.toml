[workspace]
members = [
    "protocol",
    "pc-app",
    "rpi-controller",
    "transport",
]
resolver = "3"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
